package test;

import data.DataCuota;
import data.DataMonto_cuota;
import entities.Cuota;
import entities.Monto_cuota;
import java.util.LinkedList;
import java.time.LocalDate;

public class TestMonto_cuota {
	public static void main(String[] args) {
		
		/*DataMonto_cuota dmc = new DataMonto_cuota();
        Monto_cuota monto = new Monto_cuota(LocalDate.now(), 1500.0, 1); 
        
        DataCuota dc = new DataCuota();
        Cuota cuotaPrueba = null;

        System.out.println("--- Agregando nueva cuota ---");
        Cuota nuevaCuota = new Cuota(1, LocalDate.now(), LocalDate.now().plusMonths(1));
        dc.add(nuevaCuota);
        System.out.println("Cuota agregada con ID: " + nuevaCuota.getId());

        // Add
        dmc.add(monto);
        System.out.println("Monto_cuota agregado para fecha: " + monto.getFecha());

        // GetAll
        LinkedList<Monto_cuota> montos = dmc.getAll();
        System.out.println("Total de montos: " + montos.size());

        // Update
        monto.setMonto(1750.0);
        dmc.update(monto);
        System.out.println("Monto actualizado.");

        // GetByFecha
        Monto_cuota montoRec = dmc.getByFecha(monto.getFecha());
        System.out.println("Monto recuperado: " + montoRec.getMonto());

        // Delete
        dmc.delete(monto.getFecha());
        System.out.println("Monto eliminado.");*/
	}

}
